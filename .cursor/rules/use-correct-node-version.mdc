---
description: Use Node 22+ via nvm before running yarn commands
alwaysApply: true
---

# Node Version

This repo requires **Node 22+** (see `.nvmrc`). Run `nvm use` before any `yarn` command.

All npm scripts call `scripts/check-node-version.mjs` as a guard, but switching first avoids confusing errors.
